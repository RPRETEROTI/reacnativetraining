import React from "react";
import * as Location from 'expo-location';
import { Alert, Button } from "react-native";

export const LocationPickerFeature: React.FC<any> = (props) => {
 
  return <Button title="imagePick" onPress={() => {}}></Button>;
};
